import 'package:flutter/material.dart';


class InfoPage extends StatelessWidget {
  const InfoPage({super.key, required data});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("infa page")),
    );
  }
}